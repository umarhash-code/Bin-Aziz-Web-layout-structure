# Bin Aziz Frontend

## Run
1. Install dependencies:
   - `npm install`
2. Create `.env` (optional):
   - `VITE_API_BASE_URL=http://localhost:5000/api`
3. Start dev server:
   - `npm run dev`

## Main Routes
- `/login`
- `/signup`
- `/courses`
- `/payment`
- `/dashboard`
- `/admin` (admin only)
